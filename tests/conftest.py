"""Test shared fixtures for the revenue recovery agent test suite."""

import sys
import os

# Ensure src/ is on the Python path for imports
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))
